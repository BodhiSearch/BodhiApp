# Two separate PostgreSQL containers (see docker/docker-compose.test.yml) because
# the official postgres image only supports one database per container via POSTGRES_DB.
# Session DB on port 54320, App DB on port 64320.
INTEG_TEST_SESSION_PG_URL=postgres://bodhi_test:bodhi_test@localhost:54320/bodhi_sessions
INTEG_TEST_APP_DB_PG_URL=postgres://bodhi_test:bodhi_test@localhost:64320/bodhi_app
