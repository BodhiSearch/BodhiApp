app_execute_error-native_not_supported = application is built with non-native variant, does not support `app` command to launch app in native mode
app_execute_error-unreachable = unreachable: {$var_0}
native_error-tauri = {$error}
